aqua Main

import "@fluencelabs/aqua-lib/builtin.aqua"

use "hosts.aqua"
import "services.aqua"

export runFibonacci

-- remote
func runFibonacci() -> string:
    on HOST_PEER_ID:
        resp <- Benchmark.fibonacci(10)
        from_peer_msg <- Debug.stringify(resp)
    <- from_peer_msg
